language: elixir

elixir:
  - 1.5

otp_release:
  - 20.0

services:
  - postgresql
addons:
  postgresql: "9.6"

install:
  - mix local.rebar --force
  - mix.local.hex --force
  - mix deps.get

script:
  - mix test
